name: Build

on:
  push:
    branches:
      - main
      - fix
    paths-ignore:
      - 'assets/**'

jobs:
  test:
    uses: clambin/workflows/.github/workflows/test.yaml@main
  build:
    needs:
      - test
    uses: clambin/workflows/.github/workflows/build.yaml@main
    with:
      target: k8s-restarter
